# Simple Storage

Simple storage application with authentication.

## Installation and Usage

```bash
cd backend && cp .env.example .env && cd .. && docker compose up --build
```

## API

```bash
http://localhost:8000/docs
```
